1000
# Issue #1917 / PR #1919 rework: the dead_worker_failure_kind drift-item
# regression tests (detect_drift emission, apply_fixes stamping, and the
# untracked-issue no-op) put the file at 829 lines -- past the 800 cap.
# Raised to the next MARK_QUANTUM multiple per the ratchet's hand-raise
# rule; the alternative (a new test module) would duplicate the
# reconcile drift fixture harness since test modules must not import
# each other.
