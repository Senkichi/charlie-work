**Repository commands take precedence.** The commands in this prompt are generic defaults. Where this repository's own `CLAUDE.md` or `CONTRIBUTING.md` documents a different command for tests, lint, formatting, or environment setup, run that one instead. If a command here fails before your code is even exercised (a missing extra or dependency group, an unknown flag), that is a mismatch with this repository's tooling, not a defect in your change: switch to the documented command and name it in your PR body. Impacted test files can live anywhere in the test tree, including nested packages such as `tests/<package>/test_<module>.py`; pass their real paths.
