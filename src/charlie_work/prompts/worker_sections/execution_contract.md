**Execution contract (final-head gate):** After the last commit, run the **FULL suite** locally at the final head before pushing (`uv run --extra dev pytest -q --tb=short`). self-detect from your diff; if the diff changes any public function signature, return shape, exception type/message consumed elsewhere, DB schema, or module re-export, the full suite is mandatory. The targeted command above is a quick scan; the full suite is the required pre-push verification gate. Do not rely on targeted runs alone. Quote the exact command you ran and the collected/passed count in the completion report (e.g., "300 collected, 300 passed").
