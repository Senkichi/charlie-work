## Spec compliance

When the issue says "use helper X" or "do NOT hand-roll Y", doing exactly that is part of the acceptance bar — even if a hand-rolled version behaves identically today. A reviewer treats deviating from an explicit instruction as a finding regardless of whether the resulting behavior passes tests.
