## Config parity

If you edit a config example file (e.g., `config.example.yaml`, `*.example.yaml`, or an `examples/*.yaml` counterpart), mirror the change to every sibling or bundled copy. Run the parity tests explicitly and verify they pass before pushing.
