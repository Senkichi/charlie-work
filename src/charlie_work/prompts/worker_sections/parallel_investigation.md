## Parallel investigation

Where the worker harness supports concurrent sub-tasks, fan out independent investigation and verification steps. Read unrelated modules, explore independent test files, and run independent verification commands in parallel rather than serializing. Use available concurrency so independent work does not block unnecessarily.
