Confirm you are on the branch above (`git branch --show-current`). If it exists,
   `git switch $branch_name`; if it does not, create it from the latest `main` with
   `git fetch origin && git switch -c $branch_name origin/main`.
