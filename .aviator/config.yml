merge_rules:
  labels:
    trigger: "mergequeue"
  preconditions:
    number_of_approvals: 0
    required_checks:
      - "Lint"
      - "Tests"
      - "Collect-only gate"
  merge_strategy:
    name: "squash"
